# Copy to .env and fill in with the OrchestrationStack's HttpApiUrl
# CloudFormation output (`aws cloudformation describe-stacks --stack-name
# Klyro-OrchestrationStack --query "Stacks[0].Outputs"`), e.g.:
VITE_API_BASE_URL=https://xxxxxxxxxx.execute-api.ap-south-1.amazonaws.com
